import type { RouteObject } from "react-router";

const profileTaggedGridRoutes: RouteObject[] = [
  {
    path: "/profile/tagged/grid",
    element: <div>Tagged Grid</div>,
  },
];

export default profileTaggedGridRoutes;
