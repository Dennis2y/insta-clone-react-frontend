import type { RouteObject } from "react-router";

const profileTaggedRoutes: RouteObject[] = [
  {
    path: "/profile/tagged",
    element: <div>Tagged Posts</div>,
  },
];

export default profileTaggedRoutes;
